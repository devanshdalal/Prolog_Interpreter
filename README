This project is developed in OCaml using OCamlYacc and OCamlLex for Parsing and Lexing.
It supports basic prolog relational Iterpretation and the back-end computations are done mainly using pattern matching  and Most General Unifier algorithms. 

software Reqirements:
	OCaml environment 
	NOTE: No Running prolog environment is required. 
Commands:
	type "make" and press ENTER to compile the project.
	execute command ./run <file containing perl code>
